# init

This repository bootstraps my environment but has unfortunately not been tested on macOS yet. It
shall be used on the next environment initialization.

```bash
curl -fsSL https://raw.githubusercontent.com/carlba/init/main/init.sh | bash
```
